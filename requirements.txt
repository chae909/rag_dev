# Core dependencies
streamlit>=1.28.0
pandas>=2.0.0
numpy>=2.1.0

# LangChain ecosystem
langchain>=0.3.0
langchain-community>=0.3.0
langchain-core>=0.3.0
langchain-openai>=0.3.0
langchain-pinecone>=0.2.0
langchain-text-splitters>=0.3.0

# Vector database
faiss-cpu>=1.8.0
pinecone-client>=3.0.0

# OpenAI
openai>=1.0.0

# HTTP clients
httpx>=0.25.0
aiohttp>=3.8.0

# Data processing
marshmallow>=3.20.0
pydantic>=2.0.0

# Utilities
python-dotenv>=1.0.0
requests>=2.31.0
tqdm>=4.65.0

# Optional: Remove if not needed
# torch  # Remove problematic torch version
# ipython  # Remove if not needed for deployment
# jupyter_client  # Remove if not needed for deployment
# debugpy  # Remove if not needed for deployment